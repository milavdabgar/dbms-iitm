Welcome back to the deep dive. You know, I was I was looking over the stack of notes we've covered recently regarding databases, and I noticed a bit of a pattern. We've sort of been acting like uh very efficient librarians.

Librarians? How so?

Well, think about it. It's always, you know, go fetch this record or finding the user with ID 405. Pull up the list of books published in 1999. It's all retrieval,

right?

Which is important obviously, but it feels a bit I don't know, passive. We're just reading what's already written.

Yeah. That is a fair assessment. You're in the uh the retrieval phase. It is essential, but it is basic. It's the difference between being able to read a book and actually being able to analyze the plot.

Exactly. But today, looking at this new source material, specifically module 10 on SQL. We're definitely done with just fetching. We're moving into the engine room.

I really are.

We're talking about manipulating logic, dealing with the absolutely terrifying concept of the unknown, and summarizing massive data sets into actual meaning.

It is a significant shift the source material frames this as the transition from a data fetcher to a data analyst. We aren't just asking what is here anymore. We are asking complex questions.

And to do that, we have three distinct tools from the source to discuss today. Set operations, null values, and aggregation functions.

And I have to say, looking at the section on null values, especially it gets surprisingly philosophical for a coding course.

It really does. It completely challenges the binary way we usually think about truth. But before we stare into the void, uh we need to start with structure. The source material opens with a concept that seems simple but changes everything about how you write a query. Thinking in sets,

right? This is the set operations part. The text basically says, stop looking at your database table as a spreadsheet or just a list of rows. You need to visualize it as a mathematical set.

Exactly. Think back to grade school math. Ven diagrams, circles representing groups of things. In SQL, when we form the specific operations union intersect except we are treating the entire table or the entire result list as a single object a circle in that vin diagram.

But there's a catch.

Yeah,

a big one. You can't just smash any two tables together and call it a set.

No, you absolutely cannot. This is the prerequisite. The source calls it being union compatible.

I like to call it the applesto apples rule.

That works too. The rule is strict. For any binary set operation, the two relations involved must have the identical structure, same number of attributes, and the data types must match in order.

So, I can't take a list of customer names and try to combine it with a list of product prices.

Right? It sounds obvious when you say it like that. You'd get a list that is half text and half numbers, which makes no sense at all. But in practice, it's a common error. Usually, you aren't combining two random tables. You are combining two derived lists that answer similar questions.

The example in the notes is university courses, which I actually found really helpful. Imagine you have a list of courses that ran in fall 2009 and you have a separate list of courses that ran in spring 2010.

Okay. So think about the structure there. Both lists just contain course IDs. They are identical in shape.

So they are union compatible. Now I can play with them

precisely. And the most basic operation is the union. This is your O logic.

Show me courses that ran in fall 2009 O spring 2010.

Exactly. You take the first pile, you take the second pile and you combine them. The union operator just puts them into one big list. Cool enough, but then it gets a bit more exclusive. What if I want to know which courses ran in fall 2009 ad spring 2010?

That is your intersect. It looks at both circles of the ven diagram and only gives you the overlapping middle.

Gotcha.

If a course was in the fall list but dropped for the spring, it doesn't show up. If a new course started in the spring, it doesn't show up. Only the survivors remain.

And the third one, which I actually think is the most useful for detective work, is accept, right? The notes define this as exclusion. Find courses that ran in fall 2009 ba not in spring 2010.

This one is incredibly powerful. You take the first set, your starting point, and you actively subtract any item that appears in the second set. It's perfect for finding drops or missing data.

Now, speaking of detective work, this is where the source material threw a curveball that I really want to unpack. It lays out a logic puzzle using these set operations. It asks, how do you find the instructor with the highest salary?

Ah, Uh, yes, but with a constraint, you cannot use the max function.

Exactly. Most people would just type select max salary and be done with it.

Yeah,

but the text says no. Do it using said operations. And the logic it lays out is honestly it felt like a riddle.

It's a lesson in negative space. If you can't point directly at the winner, how do you find them?

You identify all the losers.

Harsh, but accurate. To find the highest salary, you first have to identify every salary that is not the highest.

Okay, so walk me through this. How do we define not the highest?

Logically, a salary is not the highest if there is at least one other salary in the table that is bigger than it.

Okay, so step one is a comparison.

Yes, you create a query that compares every salary to every other salary. If salary A is less than salary B, then salary A goes onto the loser list. It is definitely not the highest.

So now I have a set of all the smaller salaries.

Correct? Now you take the set of all possible salaries, the entire list, and you use the except operator to subtract the loser list

and the only thing left standing

is the one salary that was never smaller than anyone else's. The highest salary.

That is such a roundabout way of thinking. It's like finding the fastest runner by disqualifying everyone who has ever been passed.

It is. But it demonstrates the power of exclusion. You find the truth by eliminating everything that isn't the truth. It forces you to think relationally, not just computationally.

Before we move off sets, there's a nuance here about duplicates that I think is critical for you to know. Cuz usually sets by definition don't have duplicates. If I have the number five in my set, I don't need it written down three times.

Correct. Standard SQL set operations union intersect except automatically eliminate duplicates. The result is always a unique list.

But sometimes, especially in finance or inventory, you want the duplicates. You need to know exactly how many times something occurred.

Enter the A all keyword. Union all intersect all. This stops the automatic cleanup.

The math on this in the notes was surprisingly specific and want to make sure I've got this locked down because it feels like a test question waiting to happen.

It is very precise. Let's walk through an example. Imagine we have table A and table B. Let's say a specific record, we'll call it item X, appears five times in table A and three times in table B.

Okay, five times in A, three times in B.

If you do union all, what happens?

Basically addition, right? You just stack them. So 5 + 3 equals 8. The result have eight rows of item X.

Correct? Simple arithmetic. Now what about intersect all?

Intersection is about what they share. So if A has five and B has three. They only share three in common.

Exactly. It uses the minimum count. The result is three.

Okay. Now the tricky one except T. All this is subtraction.

It is. If you take table A which has five and except table B which has three.

5 - 3 is two. So I'm left with two rows.

Right. But and this is the edge case that trips people up. What if we flip it? What if table A has three and table B has five?

Well 3 - 5 is -2. But you can't have negative rows in a database table.

Precisely. You cannot have a negative count of rows. So the result is strictly zero.

The database just wipes the slate clean.

It floors at zero. So if you're running a query expecting to see a negative balance to flag an error except all won't show it to you, it just disappears.

Which is a perfect pay segue actually

to things disappearing

to the concept of the void, the unknown. Yeah,

we're moving to section two. Null values.

Uh yes, nulls. This is probably the most misunderstood concept in database management.

The source material is remarkably empathetic here. It says, "Look, sometimes a value just doesn't exist, like a student who doesn't have a passport number yet. It's not that their passport number is zero."

That distinction is vital. Zero is a value. Zero credits means you took classes and earned nothing. Or 0 degrees is a temperature. Null means the value has not been set. It is the literal absence of information.

Right? And in standard coding like C or C++, null is often just a zero or a special literal. But in SQL, This absence acts almost like a virus. The text mentions the arithmetic effect.

The virus effect. I like that.

If I try to do math with a null value, what happens?

The unknown swallows the known. If you take the number five and add null, the result is null. If you multiply a million dollars by null, the result is null. Because if part of the equation is unknown, the result must be unknown.

That makes total sense, but it's dangerous if you aren't expecting it. And it gets even trickier when you try to compare things. I feel like this is a trap everyone falls into. writing something like where salary equals innull.

Yes. So that is the classic syntax error and logic. You simply cannot use the equal sign with null.

Why not?

Because equals implies you're comparing two specific known quantities. Is 5 equal to 5? Yes. Is 5 equal to four? No. But if I ask is five equal to unknown, what is the answer?

I don't know

exactly. You can't say true and you can't say false. The answer is unknown.

So if I write salary equals equals nol SQL doesn't return true,

it returns null, which functionally acts like a roadblock. The correct syntax is strictly is nll or I is not nll. You have to ask about the state, not the value.

This leads us to the part of the source that felt the most dense, but also the most rewarding once I got it. The three value logic. We're used to true and false. But SQL introduces the third state, unknown.

Boolean logic, the logic of computers, usually assumes a binary world. But data reflects the real world. And in the real world, I don't know is a valid answer. We have to define how that I don't know interacts with true and false.

So let's run through the logic tables provided in the source. I want to roleplay this a bit because it helps to hear it.

Let's do it.

Okay, let's start with or logic. I have a statement. My car is red or unknown. Now let's say it is a fact that my car is red. What is the result of that statement?

The result is true.

Why? There's an unknown in there

because or logic is generous. It only requires one side to be true. Since we know your car is red, it doesn't matter what the unknown part is, the statement is satisfied.

Okay, that clicks. Now, let's try A and D. My car is red and unknown.

That results in unknown.

Why not true? The car is red.

Because A and D is strict. It requires both sides to be true. We know the car is red, but we don't know if the other condition is met. So, we can't say it's true, but we also can't say it's false yet. We are stuck in limbo.

Okay, last one. False unknown.

That is definitively false

because the false side kills it immediately. Exactly. If one side is false, Amy can never be true. The unknown part is completely irrelevant.

It's surprisingly intuitive once you map it out. But it has huge implications for filtering data. The source mentions specifically the wear clause. It calls it the gatekeeper.

This is the most practical takeaway from as a listener. The wear clause decides which rows get into your result. Crucially, it only lets in rows where the condition is definitively true.

So if the condition evaluates to false,

rejected,

and if ital is the unknown

rejected.

That is the kicker. Unknown is treated as a rejection.

Think about the implication. If you say, "Show me everyone with a salary greater than 50,000." Anyone with a null salary is dropped.

Makes sense.

But if you run a second report, show me everyone with the salary LSS than or equal to 50,000. The nulls are also dropped.

They vanish from both lists.

Yes. Because they are neither greater than, less than, nor equal to. They fall into the cracks between the logic.

That is It's spooky. It's like dark matter. It's there. You can't see it with standard tools,

which is exactly why handling nulls, explicitly checking is null is so important. Otherwise, you're analyzing incomplete data and you don't even realize it.

All right, let's move to the final piece of the puzzle. We've filtered our sets using logic. We've navigated the void of nulls and now we have a pile of data. It's time to make sense of it with aggregation.

We are moving from rowbyrow analysis to summarizing multi- sets. We have the standard tools, average, min, max, X, sum, and count.

And count seems to be the popular one. But the source points out a major nuance between count star and count column name.

It loops right back to our null conversation. Count star counts the rows. It doesn't care what's in them. It just counts the existence of a record. So if you have a row completely full of nulls, count star still counts it as one.

It counts the bag, not the contents,

right? But count column name checks the value in that specific column. And here's the rule. Aggregation functions ignore null. So if I have a 100 students but only 80 have a grade assigned and I run count grade,

you get 80. If you run count star, you get 100.

That is a discrepancy that could get someone in serious trouble if they're reporting on compliance or enrollment.

Absolutely. You have to know what you are counting.

Now the real heavy lifter here is group by Y. I love the visualization the source gives for this. It's like taking a big table and cutting it into subtables.

Yes. Imagine the instructor table. You want the average salary, but not for everyone. You want it per department. group by department slices the table. All the computer science folks go into one pile, all the history folks into another pile,

and then the aggregation function like average runs on each pile separately.

Correct. But there is a syntax rule here that trips up almost every beginner. I see it constantly.

I know exactly what you're talking about. The source highlights this in bold. If you use group by department, your select line can only contain department and your aggregation function.

You can't just throw name or ID in there.

Why not? I mean, the data is still there.

Think about the logic. You have a group of 10 history professors. You've squashed them into a single summary row to show the average salary. If you ask the database to also show name, which name should it display?

There's 10 of them.

Exactly. It's ambiguous. So, the database throws an error. It refuses to guess. If you group by department, the only things you can display are the department itself and the summary math. The individual details are lost in the compression.

That makes total sense. You can't list an individual ID next to a departmental average. It just doesn't fit

exactly.

There's one last distinction in aggregation that I think is critical for anyone trying to query data. The battle between where and having. They both filter things. So why do we need two?

It comes down to when they happen. Think of the timeline of a query.

Okay, timeline.

First, we grab the raw data. The wear clause runs, right? Then it filters the individual rows before any grouping happens.

So only look at professors in the science building. building that happens first,

right? The wear clause is the bouncer at the front door. It decides who gets into the building. Then the survivors are grouped into piles. We calculate the averages. Now we have a new set of data, the summaries.

And that's where having comes in.

Yes. Having filters the groups after the aggregation is done.

So that would be like show me departments where the average salary is over 100K.

Exactly. You can't use where for that because the average salary doesn't exist yet when wear runs. You haven't done the math yet. You need having to filter the result. of the aggregation.

That clears up so much confusion. Where is for rows? Having is for groups.

And just to tie it back to our previous topic, nulls in aggregation. Generally, functions like sum, ag, min, and max simply ignore nulls. They calculate based on the data that actually exists.

What if a group contains only null values? Say we have a future studies department where nobody has a salary negotiated yet.

If the entire set is null, the result of the aggregation is no.

So a sum of a null, null, null isn't zero. No, it is null because the sum of unknown quantities is still unknown

except for count,

right? Count would return zero because it can definitively say I found zero values.

It all comes back to that logic. Zero is a number. Null is a state of mind.

Well, a state of logic, but yes.

So, what does this all mean for you listening? We've gone from combining sets using strict rules to navigating the treacherous waters of missing information to finally slicing and dicing data into summaries.

We've effectively moved from data entry to data science. These tool set operations, logic handling, and aggregation are the bedrock of answering complex questions. You aren't just retrieving phone numbers anymore. You're asking who is paid the most relative to their peers or which departments are outliers.

It really highlights that SQL is a thinking tool, not just a typing tool.

And specifically, it forces you to confront the unknown. In the real world, we often conflate I don't know with no or zero.

Right? If I asked you have $5 and You say I don't know. I usually just assume you don't.

But SQL forces us to hold unknown as a distinct logical state. It forces accuracy. If you don't know the value, you cannot say it is greater than, less than, or equal to anything.

That is actually a really profound thought to leave you with. In your own real life decision-making, how often do you treat a lack of information as a false or a zero instead of acknowledging it as a third state that requires special logic?

It is a question worth asking. Accuracy requires ad what you don't know.

Well, on that note, we're going to wrap up this deep dive. Whether you're joining sets, filtering groups, or just trying to avoid the nulls in your life, thanks for exploring this with us.

Keep analyzing your data.

See you next time.