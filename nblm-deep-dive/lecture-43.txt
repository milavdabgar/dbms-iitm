Welcome to today's deep dive. Uh we are really thrilled to have you with us today.

Yeah, absolutely. Thanks for joining us.

And um our mission for today is to decode something you interact with hundreds of times a day, probably without even realizing it.

Definitely every time you pick up your phone really,

right? We are looking at how computer databases actually organize and store the the massive amounts of information we rely on. And to do this, we are pulling from a really fantastic source.

It's a lecture transcript from the IIT Madress B degree program on algorithms and data structures

which is just a phenomenal foundation for understanding the physical layer of data management. I mean we're moving beyond the abstract here and and looking at how data is literally structured in a computer's memory.

Exactly. And to set the stage for you just uh picture this relatable scenario. You type a query into a search engine or you look up a specific customer in some massive corporate database.

Right.

How is it that the database can instantly find that single piece of data among millions or even millions of records without completely crashing your system.

What's fascinating here is that the way data is structured physically dictates the speed and efficiency of absolutely everything we do online. Today we are graduating from simple flat linear lists to complex multi-layered networks.

Right. Because before we can appreciate the really complex stuff, we have to talk about the friction with the simple stuff.

Yeah. You have to know what's broken before you fix it.

Exactly. Since you are likely already familiar with the basics of linear data structures, specifically arrays and linked lists. We don't need to rehash what they are.

No, we don't need to go back to square one.

But we do need to look at how they break down at scale to establish exactly why we need to evolve past them. When we talk about database applications, there are basically four primary operations we care about. Access, search, insert, and delete.

And this is exactly where we hit the great trade-off. Let's look at arrays first. Arrays are excellent for accessing data quickly. If you you know exactly the index where a piece of data lives, you can grab it instantly,

right? It's right there.

But think about what happens when you try to insert or delete data in an array with millions of records.

Wait, so if arrays are so fast for finding things by their index, why do they just lock up when we try to add something new?

Because you have to physically move the data around to maintain the sequence.

If you want to insert a new item at the very beginning of an array, you have to shift every single other item down once. bot to make room.

Oh wow.

Yeah. So if your array has 10 items, that's trivial. But if your array has 10 million customer profiles,

oh man,

shifting all of them just to add a new user creates a massive bottleneck.

That makes a lot of sense actually. So flip that around and look at linked lists. Linked lists solve the insertion problem beautifully that you just create a new node and update a couple of pointers. You don't have to physically move the rest of the data at all. It just takes a constant number of reference movements, which engineers call order one. complexity,

right?

But then you run into the opposite problem. Searching becomes a total nightmare.

Exactly. Because you have to start at the very beginning and follow the pointers one by one until you find what you're looking for. You lose that instant access capability that arrays have.

Yeah.

But there's another massive hidden cost with linked lists that often gets overlooked when you're working at scale.

Oh, the space overhead. This detail from the lecture really jumped out at me.

It's wild, isn't it?

It really is. Both arrays and linked lists take up space. space that grows proportionally with the amount of data. What's mathematically called O of N space complexity. But a length list can sometimes have a 100% space overhead.

It is a staggering inefficiency. When you really think about it, every single data node in a linked list requires a pointer attached to it just to tell the system where the next piece of data is.

Just to point to the next thing.

Exactly. If the data you're storing is small, like a single integer or a tiny text flag, the pointer might take up just as much memory as the itself. You are effectively doubling your physical space requirements just to maintain the structure of the list.

Okay, let's unpack this. We can't use arrays efficiently for dynamic databases because they grind to a halt when we try to add or remove data, right?

And we can't use linked lists for everything because they take forever to search and they just eat up massive amounts of memory with structural overhead.

Yeah,

we clearly need a compromise.

We do. And finding that compromise requires us to completely stop thinking in straight lines. We have to move into nonlinear data structures.

Which means what exactly?

Well, instead of a single sequence where one item simply follows the next, like people in line at a grocery store, data elements in a nonlinear structure are organized through hierarchical linkages.

There are multiple interconnected paths to get from one element to another.

Okay, let's talk about graphs first. Then at its core, a graph is essentially a collection of vertices, which are your data points, and edges, which are the lines connecting them.

Yes.

And graphs can have all sorts of properties depending on what you are actually trying to model.

Think about real world networks to ground this for a second. A water sanitation grid is a massive graph. The intersections or pumping stations are the vertices and the pipes are the edges.

Okay?

And those pipes might have specific capacities which in graph theory we call weights. If the water can only flow in one direction, that's a directed graph. If it can flow both ways, it's undirected.

And social media relies heavily on this too. Your network of friendships on Facebook is an undirected graph. If you're friends with someone, they are automatically friends with you. The connection goes both ways,

right? It's mutual.

But Twitter or Instagram is a directed graph. You can follow someone without them following you back. The arrow only points one way.

That is a perfect way to visualize it. You also have entity relationship diagrams or ER diagrams which map out complex database architectures. Even the massive knowledge graphs used by search engines to connect concepts together are all fundamentally graph structures. They really are absolutely everywhere. But within this massive world of graphs, there is a very special, highly constrained type of graph that solves our database problem. Trees.

Yeah, trees.

Now, for a graph to graduate to being considered a tree, it has to follow two strict rules. First, it must be connected. That means every single node is reachable from the starting point. You can't have isolated islands of data just floating off on their own, right?

Second, it must be a cyclic. Meaning there are no loops or cycles. You can't trace a path through the data and somehow end up back where you started. It is a strict branching hierarchy.

Right? Think about how a computer represents a composite attribute. The main node might be name and then it branches down into first, middle, and last. Yes. Or date branching down into year, month, and day. Even a standard genealogy family tree is quite literally a tree data structure in computer science.

And just to clarify, a fun piece the terminology here. If you do happen to have a collection of trees that are not connected to each other, just a bunch of separate hierarchies sitting in the same database, that collection is actually called a forest.

I love that. It's really rare that computer science naming conventions are that intuitive.

Now, we should quickly mention that there is another major nonlinear structure out there called hash tables. They use mathematical functions to map keys directly to an index,

which is fascinating, but

but it is a massive topic on its own, usually covered in advanced modules. The source lecture mentions will hit that in module 44. So we're going to save hashts for a future deep dive. Today we are strictly sticking to the trees.

Sounds good. And to really understand how a tree gives us that perfect compromise between searching and inserting, we need to understand its anatomy. We need to build a mental image of how this looks physically.

Let's build one right now. At the very top of our hierarchy, we have a single starting point. This is the root. It's the only node in the entire structure that has no parent above. of it.

From the root, you branch downward. The nodes you connect to are the children. If those children also have branches extending beneath them, they are called internal nodes. They sit somewhere in the middle of the hierarchy.

And when you follow a path all the way to the bottom until it just stops branching. You hit a leaf. A leaf is simply a node with no children. It is the end of the line.

There is also a limit to how many branches a node can have, which is called its arity. For database structures, we usually focus on a binary tree, meaning it has an arity of two. Every node is allowed a maximum of two children, usually referred to as a left child and a right child.

And finally, we have to talk about levels and height. The root sits at level zero. Its direct children are level one. The grandchildren are level two and so on. The height of the entire tree is determined by tracing the longest possible path from the root down to the furthest leaf.

If we connect this to the bigger picture, the height of a tree is the single most important factor in a database's performance. The height direct dictates how fast you can search. The shorter the tree, the faster the search.

Why is that exactly? Let's skip the dry formulas and just look at the raw mechanics of how this scales. If we have a binary tree where every single node can have two children, the number of nodes we can fit at each level essentially doubles as we go down.

That is the magic of exponential growth. At level zero, you have just the root, one node. At level one, you can fit two nodes. At level two, you can fit four nodes. Then 8, 16, 32, and so on.

So if you want to know the absolute maximum capacity of a tree. You just add all those levels up. By the time you get down to just level 30, you can hold over a billion data points.

And this mathematical reality creates a performance boundary. In the absolute best case scenario where your tree is perfectly filled out, meaning every node has two children and it's completely balanced, the search time is incredibly efficient. We call this O of log and efficiency,

which is just a fancy mathematical way of saying you get to skip looking at almost every Think of it like this. If you have a perfectly balanced tree, every time you make a choice to go left or right, you are instantly Thanos snapping half of the remaining data out of existence.

I love that analogy.

That's what O of log N means in plain English. You can search a billion records and by just making 30 left or right decisions, you'll find exactly what you're looking for.

30 steps to search a billion records. Contrast that with searching a linked list, which is an O of operation. To search a billion records in a linked list in the Worst case, you have to take a billion steps.

Here's where it gets really interesting. How do we actually organize the data inside this tree so the computer knows whether to go left or right?

Oh, yes.

This brings us to the star of the show, the binary search tree or BST. There is a golden rule for building a binary search tree. For any given node, all the values in its left subree must be mathematically lesser than that nerd's value. And all the values in its right subree must be mathematically greater.

We really need Applause on this rule because it is a massive trap that trips up a lot of people, especially in software engineering job interviews.

I have heard about candidates totally bombing the whiteboard test on this exact concept.

It happens constantly. People often mistakenly think this lesser or greater rule only applies to a node's direct children. A candidate will draw a tree and think, "Okay, as long as the left child is smaller and the right child is bigger, I've built a valid binary search tree." But that is fundamentally wrong,

right? because it applies to the entire subree. Every single node that eventually branches off to the left, no matter how deep it goes, must be smaller than the root. Every single node branching anywhere to the right, must be larger.

If even one node sitting 10 levels deep in the left branch, is somehow larger than the root node at the top, the entire structure is completely invalid. The search algorithm will fail because it's making assumptions about the data that just aren't true.

To really see how this works and where it can go wrong, let's look at a specific example of Building a tree from scratch. The lecture gives a great one. Let's say we want to insert the letters for the word elephant into a fresh binary search tree. We'll drop the duplicate E. So we just have E L P H A N T.

This is a perfect exercise to see the mechanics in action.

Okay, we start with the letter E. It's the first piece of data. So E becomes our root node. Next comes L. Alphabetically, L is greater than E. So following our golden rule, L becomes the right child of E. So far so good.

Next is P. We start back at the root. E. P is greater than E, so we go right. We hit L. P is also greater than L, so it goes to the right of L.

Now we add H. Start at E. H is greater than E, so go right. We hit L. But H is less than L, so H branches off to the left of L. Next is A. Start at the root. A is less than E, so it immediately goes to the left of E. That is actually our very first left child off the root.

We summarize the rest of the process. Inserting N and T. follows the exact same logic. N goes right from E right from L and then left from PT goes right down the whole chain to sit at the bottom right.

But wait, if we step back and look at the physical tree we've just built, we have a major problem. We have essentially built a lopsided tree. If you look at the left side of our root node E, we only have one single node sitting there, the letter A.

While the right side is completely overloaded, LPHN and T are all crammed onto the right branches.

This introduces a dangerous concept, an unbalanced tree. Because we ins ed the data in a somewhat alphabetical order. The tree heavily skewed to one side.

And an unbalanced tree completely destroys the efficiency we were just talking about. Let's test this lopsided tree by trying to search for a letter that isn't even in there. Let's search for the letter Q.

Okay, the system starts at the root. E. Q is greater than E, so it goes right to L. Q is greater than L goes right to P. Q is greater than P goes right to T. Q is less than T, so it tries to go left. But T is a leaf. It has no children. The search hits a dead end and fails.

Look at how much work that took because all the data was crammed onto the right side. The system couldn't fos snap half the data away at each step. It basically had to check almost every single node in the sequence. The number of comparisons required wasn't the efficient O of log N we wanted. It devolved into checking the height of the tree plus one.

So if we pull up the final scorecard for a basic binary search tree, the results are really mixed. In the best case scenario, if we got lucky and our data naturally organized itself into a perfectly balanced tree, we hit database nirvana. Search, insert, and delete all operate in that lightning fast O of log end time.

But the worst case scenario is terrifying for a database engineer. Imagine if you inserted data that was already perfectly sorted. A B CDE E.

Oh wow. A is the root. B is greater so it goes right. C goes right of B do goes right of C. You haven't built a tree at all.

Exactly. You have accidentally built a straight line. The tree devolves back into a glorified link list and all your performance metrics drop from that. lightning fast speed back to the sluggish, painful o of end time. You are back to checking every single item one by one.

This raises an important question. How do databases prevent that from happening? Because you certainly can't rely on luck. You can't just hope that user data comes in a perfectly randomized order to keep the tree balanced.

You absolutely cannot. And while basic binary search trees allow us to traverse data in cool ways like exploring depth first or breath first, they still have that fatal flaw. They can become unbalanced. That is why modern databases don't use basic binary search trees. They use self-balancing trees

like red black trees or B trees.

Precisely. We won't dive into the mathematical gymnastics of how they work today, but the concept is brilliant. As new data is inserted, if a red black tree detects that it's getting lopsided, it actively rotates its own nodes. It automatically rearranges itself on the fly to ensure it never becomes heavily skewed.

So, they guarantee that coveted O of log end speed every single time, regardless of what order the data arrives in, without the massive case overhead of a linked list. So what does this all mean? It means that moving from linear lists to hierarchical trees gives our computers the holy grail of data management. We get the incredibly fast searching capabilities of an array combined with the fast inserting and deleting capabilities of a linked list. But and this is the vital catch, it only works if the data structure is meticulously balanced. If it loses its balance, it loses its speed entirely.

It's a delicate architecture and it is the hidden engine power powering almost every digital interaction you have. Thank you so much for joining us on this deep dive.

Yes, thank you for listening.

But before we wrap up completely, considering how meticulously a database must balance its trees to retrieve information efficiently, I want to leave you with a thought. Think about human memory as a physical data structure. Given how you can instantly recall a vivid memory from decades ago through a complex emotional linkage like a specific smell or a song, but you completely struggle to sequentially retrieve what you had for dinner 3 days ago. What kind of structure is our brain using? Are we running on highly unbalanced trees? Are our minds vast associative hashts? Or we relying on an entirely different, highly complex, nonlinear graph that computer science hasn't even mapped yet? Something to ponder the next time you try to remember a forgotten password.